## Simple Migration

Develop cli tool for shopify to prestashop product migration


### Usage

```bash
python main.py --source_url http://source.com --source_token SOURCE --target_url http://target.com --target_token TARGET
```